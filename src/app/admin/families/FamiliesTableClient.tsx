'use client';
import { useState } from 'react'; import { useRouter } from 'next/navigation'; interface Family { id: string; slug: string; category: string; name: string; downloads: number; views: number; created_at: Date }export default function FamiliesTable({ families }: { families: Family[] }) { const router = useRouter(); const [deleteModal, setDeleteModal] = useState<{ show: boolean; family: Family | null }>({ show: false, family: null }); const [deleting, setDeleting] = useState(false); const handleDeleteClick = (family: Family) => { setDeleteModal({ show: true, family }) }; const handleDeleteConfirm = async () => { if (!deleteModal.family) return; setDeleting(true); try { const response = await fetch(`/api/admin/families/${deleteModal.family.slug}`, { method: 'DELETE' }); if (!response.ok) { throw new Error('Failed to delete family') } setDeleteModal({ show: false, family: null }); router.refresh() } catch (error) { alert('Error deleting family'); setDeleting(false) } }; const handleDeleteCancel = () => { setDeleteModal({ show: false, family: null }) }; return (<div><div className="flex items-center justify-between mb-8"><div><h1 className="text-3xl font-bold text-gray-900">Manage Families</h1><p className="text-gray-600 mt-1">{families.length} families in total</p></div><a href="/admin/families/new" className="inline-flex items-center px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white rounded-lg font-medium transition">+ Add New Family</a></div><div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden"><table className="min-w-full divide-y divide-gray-200"><thead className="bg-gray-50"><tr><th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Name</th><th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Category</th><th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Downloads</th><th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Views</th><th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Created</th><th className="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th></tr></thead><tbody className="bg-white divide-y divide-gray-200">{families.length === 0 ? <tr><td colSpan={6} className="px-6 py-12 text-center text-gray-500">No families found.</td></tr> : families.map(f => <tr key={f.id} className="hover:bg-gray-50"><td className="px-6 py-4 whitespace-nowrap"><div className="text-sm font-medium text-gray-900">{f.name}</div><div className="text-sm text-gray-500">{f.slug}</div></td><td className="px-6 py-4 whitespace-nowrap"><span className="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-blue-100 text-blue-800 capitalize">{f.category}</span></td><td className="px-6 py-4 whitespace-nowrap text-sm text-gray-500">{f.downloads.toLocaleString()}</td><td className="px-6 py-4 whitespace-nowrap text-sm text-gray-500">{f.views.toLocaleString()}</td><td className="px-6 py-4 whitespace-nowrap text-sm text-gray-500">{new Date(f.created_at).toLocaleDateString()}</td><td className="px-6 py-4 whitespace-nowrap text-right text-sm font-medium"><div className="flex items-center justify-end space-x-3"><a href={`/revit/${f.category}/${f.slug}`} target="_blank" rel="noopener noreferrer" className="text-blue-600 hover:text-blue-900">View</a><a href={`/admin/families/${f.slug}/edit`} className="text-green-600 hover:text-green-900">Edit</a><button onClick={() => handleDeleteClick(f)} className="text-red-600 hover:text-red-900">Delete</button></div></td></tr>)}</tbody></table></div>{deleteModal.show && deleteModal.family && <div className="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50"><div className="bg-white rounded-xl shadow-2xl p-6 max-w-md w-full mx-4"><h3 className="text-xl font-bold text-gray-900 mb-2">Delete Family</h3><p className="text-gray-600 mb-6">Are you sure you want to delete <span className="font-semibold">{deleteModal.family.name}</span>? This action cannot be undone.</p><div className="flex items-center justify-end space-x-3"><button onClick={handleDeleteCancel} disabled={deleting} className="px-4 py-2 border border-gray-300 rounded-lg hover:bg-gray-50 transition">Cancel</button><button onClick={handleDeleteConfirm} disabled={deleting} className="px-4 py-2 bg-red-600 hover:bg-red-700 disabled:bg-gray-400 text-white rounded-lg transition font-medium">{deleting ? 'Deleting...' : 'Delete'}</button></div></div></div>}</div>) }